.vce-image-gallery {
  &--gap-$selector {
    .vce-image-gallery-list {
      @if $gap != false {
        margin-left: -$(gap)px;
        margin-right: -$(gap)px;
        margin-bottom: calc(-2 * $(gap)px);
      }
    }
    .vce-image-gallery-item {
      @if $gap != false {
        padding-left: $(gap)px;
        padding-right: $(gap)px;
        margin-bottom: calc(2 * $(gap)px);
      }
    }
  }
}
