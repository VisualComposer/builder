window.vcvAddElement(
  {{ settings() }},
  // Component callback
  function(component) {
    // {{ cssFile() }}
    component.add({{ elementComponent() }});
  },
  // css settings // css for element
  {{ cssSettings() }},
  // javascript callback
  {{ jsCallback() }}
);
